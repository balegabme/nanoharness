# Improvements

Flaw and improvement ledger (plan §4 rule 5). The `log_improvement` tool
appends entries under dated headings; the harness-editor ticks one off with a
ref once it is fixed.

In this repo the ledger is this file. In any other workspace it is
`.nanoharness/improvements.md`, because the installed package directory is
never written to.

## 2026-09-04

- [x] The OpenAI request omits `stream_options: {"include_usage": true}`, so real OpenAI never sends the final usage chunk and every recorded turn reads as zero tokens. (fixed: src/providers/openai.ts)
- [x] `nh doc-check` reported "no problems" when the target had no `src/` or `docs/harness/` at all, so a wrong path would pass the CI gate. (fixed: src/cli/doc-check.ts)
- [x] A failed usage-log write rejected the whole IPC turn after the model had already answered. (fixed: src/main/index.ts)
- [x] The `nh` CLI had no top-level error handling, so any throw surfaced as an unhandled rejection. (fixed: src/cli/index.ts)
- [x] A bash command killed by the 60s timeout reported `exit code ?` instead of saying it timed out. (fixed: src/tools/bash.ts)
- [x] The README claimed Anthropic support "from day one" when no Anthropic provider exists. (fixed: README.md)
- [ ] The provider has no request timeout and no retry: a hung connection hangs the session with no way out except quitting the app. Needs a stall deadline on the SSE reader plus backoff on 429 and 5xx (src/providers/openai.ts).
- [ ] There are no tests, and `vitest --passWithNoTests` keeps CI green anyway, so the badge on a public README currently proves only that the code compiles. Plan §16 names the strategy: recorded fixtures for the SSE and schema translation layers, smoke tests against a mock provider.
- [ ] Running out of `maxToolRounds` emits `session.finished` like a normal completion. The last round's tool results are never sent back to the model and nothing tells the user the turn was cut short (src/core/session.ts).
- [ ] `parseWire` assigns `JSON.parse` straight into a typed variable, so a malformed provider payload is trusted rather than rejected (src/providers/openai.ts). (partly fixed 2026-09-13: the usage object goes through `parseUsage`, which rejects a report missing its totals or contradicting itself; the delta shapes are still trusted.)
- [ ] An unknown tool name returns a result without `isError` or `content`, unlike every other error path in the loop (src/core/session.ts).
- [ ] `usage.jsonl` grows without bound. It needs rotation or a size cap before long-running installs accumulate years of turns (src/core/usage-log.ts).
- [x] `pnpm start` launched Electron with no window, so `window-all-closed` never fired and the app could not quit on its own. (fixed: src/main/index.ts, src/renderer/index.html)
- [ ] The preload ships as an ES module, which forces `sandbox: false` on the window. Building it as CommonJS instead would let the sandbox go back on; context isolation covers the bridge either way (src/main/window.ts).
- [ ] Main and renderer share one tsconfig, so `lib` carries DOM types into main-process code that has no business using them. Split the two once the renderer grows (tsconfig.json).
- [ ] The `read` tool's `offset` is a zero-based line index and the tool description does not say so, so a model that thinks in line numbers reads one line off (src/tools/read.ts).
- [x] Settings could only be reached when they were broken; there was no way back into the setup screen to change the model or rotate the key. (fixed: src/renderer/index.ts. Settings chip in the header)
- [ ] The key is encrypted with Electron `safeStorage`, not the OS credential store plan §11 asks for (Windows Credential Manager / Keychain / libsecret). safeStorage is DPAPI-backed on Windows and good enough to ship, but a key encrypted under one OS user cannot be read by another, and the failure is silent, since setup simply asks again (src/main/config-store.ts).
- [x] Setup accepted any base URL and model without checking they exist. (fixed: src/providers/openai.ts, src/main/config-store.ts, src/renderer/index.ts. Test connection plus a model list the user ticks). Capability warnings per model, the rest of plan §11, are still to come.
- [ ] Saving new settings retires the live sessions, but the transcript on screen still shows the old session's turns with no marker that history restarted (src/renderer/index.ts).
- [ ] `harnessEditorCwd()` exists and is tested by hand, but nothing calls it yet: the harness-editor agent itself is build step 3 (src/core/roots.ts).
- [ ] The model picker has no capability warnings. Plan §11 wants per-model checks (no vision, no tool use, no effort levels) from Anthropic `capabilities` or a one-token probe on OpenAI-compatible servers (src/renderer/index.ts).
- [ ] Switching the active model means opening settings and saving. The header chip shows the model but is not a picker, even though the allowlist is right there (src/renderer/index.ts).
- [ ] There is no electron-builder configuration yet, so the brand mark is the window icon but not the installer or executable icon (package.json).
- [ ] `Menu.setApplicationMenu(null)` also removes the accelerators that came with the default menu. Text editing is handled by Chromium so copy and paste still work, but a deliberate keyboard map (plan §13) has to register its own shortcuts (src/main/window.ts).
- [ ] With the environment variables gone, a machine whose OS has no secret store (Linux without libsecret) cannot save a key at all, so the app is unusable there. It needs a real fallback instead of a refusal: an OS-agnostic encrypted store, or an explicit, clearly labelled plaintext opt-in (src/main/config-store.ts).
- [ ] Nothing can configure a provider without the GUI, so headless runs and CI have no way in. A `nh config` subcommand writing the same files is the missing half (src/cli/index.ts, src/main/config-store.ts).
- [x] Sessions still run in `process.cwd()`, whatever directory Electron was launched from. A session has no root of its own, so nothing scopes a tool to the folder an agent was invoked in. (fixed: src/core/scope.ts, src/main/workspace-store.ts, src/main/permission.ts, src/main/index.ts)
- [ ] The model picker has no capability warnings, and the Anthropic side could read them straight from `GET /v1/models` rather than probing (src/providers/anthropic.ts, src/renderer/index.ts).
- [ ] `reasoning_effort` is passed through verbatim on the OpenAI wire. Plan §11 wants a per-family allow-list, because o1-mini takes no effort at all and other families reject values outside their own set (src/providers/openai.ts).
- [ ] Nothing sends `cache_control` on the Anthropic wire, so prompt caching never engages and `cacheWrite` stays zero on that provider (src/providers/anthropic.ts).
- [ ] Effort is one global setting. Plan §11 wants it per agent, alongside a per-agent provider and model (src/core/config.ts).

## 2026-09-05

- [x] The bash tool screens a command by pulling absolute, `~` and `../` tokens out of the string. That is a screen, not a sandbox: a path built from a variable, a subshell, a `cd` earlier in the command, or a symlink created mid-command all slip past it. Real containment needs the command run inside a sandbox that cannot see outside the root (src/tools/bash.ts). (fixed: the screen is gone. src/core/scope.ts resolves a path argument and nothing else; src/tools/bash.ts asks `access.checkCommand` and runs the command only when the gate approves it whole, which in the app means the person sees the command and answers allow once, allow all shell commands for the session, or deny. Once allowed, the shell can reach anything the user can, which is the trade deepseek-harness makes; the sandbox below is still the open half.)
- [x] A git-bash style path (`/c/blockchain/x`) resolves to `C:\c\blockchain\x` on Windows, so it is refused as outside the root but the path named in the refusal is not the path the user meant. (fixed: src/core/scope.ts, where `nativePath` translates `/<letter>/…` to a drive path on win32 and every gate goes through `normalizeTarget`; a real POSIX path like `/usr/bin` is untouched.)
- [x] The parent agent invented the mechanism when handing work over: asked to install an MCP server, it wrote "an HTTP entry with `url` and `tokenEnv` fields … decide the correct layout" into the task. `tokenEnv` was its own guess, it contradicted an endpoint that authenticates by query string, and the subagent spent 15 rounds and 130k tokens settling a contradiction nobody had, with the doc that says the file need not be hand-written already in its context. The same request with the mechanism left out was 4 rounds. (fixed: src/core/prompt.ts now says do not invent a fact, use what the project already has, stop at the outcome; in src/core/agents.ts the handoff routes harness work to the editor, src/tools/spawn.ts states the task as the outcome with the user's words quoted and never the file, fields or command, and the harness editor reads a task that names one as a guess to correct in a line.)
- [x] Thinking on the OpenAI wire was streamed to the window and then dropped: the blocks were only ever built from Anthropic's signed ones, so a session on any other provider stored no reasoning at all, and a subagent that burned 12k reasoning tokens deciding to do the wrong thing left no trace of the deciding. (fixed: src/providers/openai.ts closes a block per round from `reasoning`/`reasoning_content`; it has no signature, so `toWireMessages` in the Anthropic provider still refuses to send it and the OpenAI one never did.)
- [x] Thinking is streamed but never stored, so a re-opened session shows the messages and tool calls with the reasoning missing. (fixed for the Anthropic wire: src/core/types.ts, src/providers/anthropic.ts, src/core/session.ts, src/main/workspace-store.ts, src/renderer/chat.ts. Signed thinking blocks are kept, replayed on screen and sent back on the wire. The same change fixed a real bug: the blocks were being dropped from the next request of a tool-using turn, which the API rejects.)
- [ ] Permission grants live in memory on the broker and nothing shows them. A session can be holding several outside paths with no list of what it may reach and no way to take one back short of restarting the app (src/main/permission.ts).
- [ ] `workspace:add` opens a native directory picker, which no headless run can drive, so the one path into the whole workspace feature is exercised by hand only. It needs a seam, with the chosen directory passed in, so a probe can add a folder the way the user does (src/main/index.ts).
- [ ] A permission request that arrives for a session other than the one on screen is auto-denied. That is the safe answer, but a background turn loses its work with nothing to tell the user why (src/renderer/index.ts).
- [ ] Reasoning text from an OpenAI-compatible server is streamed to the screen and then dropped. Nothing on that wire signs it or requires it back, so there is no correctness bug, but a replayed session still shows no thinking on that side (src/core/session.ts, src/main/workspace-store.ts).
- [ ] Stop aborts the HTTP request, but a `bash` child process that is already running keeps running to completion; the loop only stops afterwards. The tool needs the session's abort signal wired to the child so a stop kills what it started (src/tools/bash.ts, src/core/session.ts).
- [x] `suspectPaths` decides what a shell command touches by pattern. It skips device nodes (`/dev/null`, `NUL`), splits on the shell's words so a quoted script body is not read as a list of paths, and takes only unmistakable shapes out of a word that is code. The underlying screen is still a guess, and the batched prompt makes a wrong guess cost one wrong answer instead of several (src/core/scope.ts). (partly fixed: the fragment prompts are gone, where `node -e "…/re/.exec(s)"` asked about `C:\.exec`, `C:\tavilyApiKey=` and `C:\` on one command line. A path spelled out of variables inside such a body is now missed rather than caught by accident.) (fixed: removed with the rest of the screen, so there is no string left to patch. The 2026-09-13 entries that describe the patches are history.)
- [x] `nh mcp check` reported "ok" for an HTTP server whose credential was wrong, because a remote server answers `initialize` and `tools/list` to anyone and only rejects at `tools/call`. (fixed: src/cli/mcp.ts, src/mcp/hub.ts. The plain form now says a handshake does not check a credential, and `--call <tool> [--args JSON]` makes one real call and prints what the server said. The tool is named by the caller: a catalog is not a list of safe things to run.)
- [ ] The alerts toggle is stored in `localStorage`, so it is per renderer origin rather than part of the configuration, and nothing syncs it with the OS notification permission if that is refused (src/renderer/notify.ts).
- [x] A renderer module that fails to load leaves a blank window and says nothing. (fixed: in src/main/window.ts, `did-fail-load`, `preload-error` and renderer console errors write a banner into the page. The main process's own stdout is discarded on Windows, so the window is the only place a launch failure can be seen.)
- [ ] `writeGuard` screens the planner's shell by pattern, so a command that builds its target at runtime, or writes through a program the patterns do not name, gets through. It is a screen for an agent that is not trying to escape, and no kind of boundary; the sandbox in plan §16 is the real answer (src/tools/bash.ts).
- [ ] A background clone gets no `job_update` tool, because adding one would change the tool definitions and invalidate the cached prefix the mode exists to reuse. So a cloned job is silent until it finishes, and the strip shows it as running with nothing to say (src/main/index.ts, src/core/spawn.ts).
- [ ] Jobs live in memory and cannot be stopped. A job that is still running when the app quits dies with the process and leaves whatever it wrote half-done, and there is no button to end one that has gone wrong (src/core/jobs.ts, src/renderer/jobs.ts).
- [ ] Switching a session's role retires the live session, so the next message rebuilds it from the transcript. The transcript keeps the old role's tool calls, and nothing on screen marks where the role changed (src/main/index.ts, src/renderer/index.ts).

## 2026-09-06

- [ ] A failed MCP server is reported only to the main process's console, which is discarded on Windows. `McpHub.status` holds the reason and nothing surfaces it, so a session simply has no search tool and never says why (src/mcp/hub.ts, src/main/index.ts, src/renderer/index.ts).
- [ ] MCP servers are configured by hand-editing `.nanoharness/mcp.json`. There is no settings screen for them, so adding a server means knowing the file exists (src/renderer/settings.ts).
- [ ] OAuth 2.1 + PKCE is unimplemented for HTTP servers. A 401 with `WWW-Authenticate: Bearer resource_metadata=…` is reported as a failed connection rather than starting the flow plan §7 describes, so a server that does not take a static bearer token cannot be reached at all (src/mcp/transport.ts).
- [ ] A dropped Streamable HTTP session is detected (404 on a known `Mcp-Session-Id`) and reported, but not re-initialized. The spec's answer is a fresh handshake; this client makes the user restart the session instead (src/mcp/transport.ts).
- [ ] MCP image and audio content blocks are replaced with a placeholder because a tool result reaches the model as a string. Anthropic takes multi-block `tool_result` content, so a vision-capable session is losing a result it could actually use (src/mcp/client.ts, src/core/session.ts).
- [ ] Every MCP server is connected when a session opens, whether or not the turn needs one. A cold `npx -y` is a package download in front of the user's first message (src/mcp/hub.ts).
- [ ] Skills are read once at session build, so a skill added to the workspace is invisible until the next session. That is deliberate, because the list is inside the cached prefix, but nothing tells the user and the reload is not offered (src/core/skills.ts, src/main/index.ts).
- [ ] Nothing checks that an MCP server's tool count is sane before its definitions go in front of every request. A server publishing eighty tools silently multiplies the per-request cost of every turn in that session, and the cost dashboard cannot attribute it (src/mcp/hub.ts).
- [ ] Saving settings retires every live session and closes its MCP servers, including one a background job is in the middle of using. The job keeps the tool objects it was built with, so its next MCP call fails on a connection that was closed underneath it (src/main/index.ts, src/core/jobs.ts).
- [ ] Plan §2 names Tavily as the default web search, added when `TAVILY_API_KEY` is set. That default is gone by decision: a harness that arrives with servers the user did not configure spawns subprocesses on their machine on its own authority, and pays for the tool definitions on every request. `examples/mcp.json` is the replacement, and the agent can write it. If the plan is to keep §2 as written, this is the divergence to argue with (docs/harness/mcp.md, src/mcp/config.ts).
- [ ] An MCP server added during a turn is not connected until the session is rebuilt, and nothing rebuilds it. The agent tells the user, but the user's only move is to switch sessions or restart the app. A "reconnect servers" action that retires and rebuilds one session is the missing half (src/main/index.ts, src/renderer/index.ts).
- [ ] Plan §7 asks for a bounded transient retry on an MCP call that fails on the transport rather than in the tool. Nothing retries: a single dropped connection is one failed tool call the model has to notice and repeat (src/mcp/hub.ts, src/mcp/client.ts).
- [x] The tool loop was capped at eight rounds, and a turn that hit the cap ended with no answer and nothing on screen to say why, which is what the analysed session actually did, mid-investigation. (fixed: src/core/session.ts. The cap is gone; an identical repeated call is refused on the third try and ends the turn on the sixth, with a note saying so, and every non-answer exit path writes a note.)
- [x] A session file held only the messages, so a re-opened session could not show what the window had shown: a stop, an error, a turn that ended empty, a background job starting or finishing. (fixed: src/core/session.ts, src/main/workspace-store.ts, src/renderer/chat.ts. The file is `{ messages, notes }`, and each note records how many messages preceded it so it comes back in position.)
- [x] Configuring an MCP server meant hand-writing JSON. An agent did exactly that, then wrote a throwaway re-implementation of this project's own parser to check its work. (fixed: in src/cli/mcp.ts, `nh mcp list/add/remove/check` writes through `parseServer` and connects through `McpHub`, and the system prompt names the commands.)
- [ ] The failure behind `nh mcp` was not only the missing command: nothing in the prompt would have named it if it had existed. Every subcommand the CLI grows has that problem and the prompt cannot list them all, so an agent needs one cheap way to ask what the harness can do (src/core/agents.ts, src/cli/index.ts).
- [ ] The repeat breaker keys on the tool name and the exact argument string, so a model that varies one character, with a different `offset` or a re-worded grep for the same thing, loops as long as it likes. Catching that needs a similarity measure, and a wrong one would refuse work that was making progress (src/core/session.ts).
- [ ] Notes are stored with the session and never redacted. Whatever a harness note quotes, a path or a command or what a failing tool printed, is written to `sessions/<id>.json` as-is, like the transcript beside it (src/main/workspace-store.ts).
- [x] The MCP prompt block handed every writing role the `nh mcp add` command while the handoff rule told that same role to delegate harness config to a harness-editor. A builder asked to install a server weighed the two in its thinking and ran the command itself, then spent three calls guessing the syntax because `nh mcp add --help` answered `unknown flag --help`. (fixed: src/mcp/hub.ts. The commands and the entry shape go to the harness-editor, which configures; a spawn-capable agent gets neither, and its session prompt routes harness work to the editor. In src/cli/mcp.ts, `--help` is answered wherever it appears and a bad flag prints the help.)
- [ ] `harnessEditorCwd()` exists and nothing calls it, so a harness-editor subagent runs with the summoning workspace as its cwd and under that workspace's access gate. Editing the harness from another project therefore raises a permission prompt for every write, and plan §5's "the harness is its workspace" is not what happens (src/core/roots.ts, src/main/index.ts).
- [x] An HTTP MCP server whose credential lives in the URL query (Tavily's `?tavilyApiKey=…` is the common one) writes that key into `mcp.json` verbatim. The file's promise that a token is only ever named holds for `tokenEnv` and not for this (src/mcp/config.ts, src/cli/mcp.ts); an agent passed the placeholder as `nh mcp add --url "…?tavilyApiKey={{secret:tavily_key}}"`, `session.ts` revealed it into the shell's arguments before the CLI ran, and the file got the live key while the tool result came back redacted and nobody could see it. (resolved by decision 2026-09-14: a plaintext credential in the harness's own config files is accepted. The boundary is the window, the transcript and the provider, and that boundary holds: substitution happens in tool arguments alone (`src/core/session.ts:640`) and every result, note and model text runs back through `redact` (`src/core/secrets.ts`). The proposed `keepsPlaceholders` writer and hub resolver are dropped. `secretsBlock` now says a config writer stores the real value and that this is expected, so an agent does not try to keep a placeholder on disk and then spend its turns proving the contradiction.)
- [x] A subagent's transcript that could not be written was discarded in a bare `catch {}`, with a comment saying the answer mattered more than the file. Both halves were true and the conclusion was not: the tool result carries `[subagent:<id>]`, so the window goes on offering to open a conversation that does not exist, and the user finds out by clicking it days later. (fixed: `SpawnDeps.problem` in src/core/spawn.ts takes the failure, src/main/index.ts routes it to the parent session so it is drawn and stored beside the turn, and with no handler it goes to stderr.)
- [ ] A session transcript that will not parse opens as an empty conversation, and the next turn writes the file back, so a corrupt or half-written `sessions/<id>.json` is silently replaced by a session with no history and what was in it is gone for good. Damage deserves a different answer from absence: the damaged file should be kept aside and the session should say what happened (src/main/workspace-store.ts).
- [ ] The secret store answers "no secrets" when the encrypted blob cannot be decrypted, which is what happens after a machine change or a different OS user. The file is still there, the keys are still in it, and the app shows an empty list, so the user's move is to re-paste keys they did not know they still had. Undecryptable is worth saying out loud (src/main/secret-store.ts).
- [x] A subagent's final answer was cut to 4000 characters, about a thousand tokens, with a `…` in place of the rest and no sentence anywhere saying so. Neither agent was told: the parent reads a tool result as the whole of what the child found, so a review whose verdict was in its last paragraph arrived as a review with no verdict. (fixed: src/core/spawn.ts. The answer is handed over whole and the harness sets no length limit on it at all; the model's own output limit is the bound, and a second one here could only ever lose the end of a finding. The job row keeps a 200-character first line, because a row is a label.)
- [x] A background subagent's answer never reached the parent's model at all. The session note carried its first line and a `[subagent:<id>]`, and a note is not a message, so the model never saw one. The answer itself was under the app's data directory, outside the workspace gate, so the agent could not read it either. An agent that started three background jobs and was asked to combine their findings had no way to do it and no way to say why: the feature looked like delegation and was a dead end. (fixed: src/core/spawn.ts. A `finished` hook hands the whole answer back; `deliver` in src/core/session.ts folds it into the conversation at every point where the transcript is balanced: the end of a round, the end of a turn, the top of the next one, and at once when no turn is running, so an answer that arrives while the user is away is stored rather than held in memory until a message that may never be sent; src/main/index.ts wires the two; the `spawn` description says the answer will arrive so the model does not go hunting for a file.)
- [x] A failed subagent transcript write was reported as a `note`, which the window draws in the margin voice and a reader takes for commentary. The harness failing at its own job is not commentary. (fixed: `fault()` in src/core/session.ts records it as an error and emits `session.error`, so it is drawn in the flow in the error style, live and on replay, with no click needed; src/main/index.ts uses it.)

## 2026-09-12

- [x] Every cached token was counted twice on the OpenAI wire. `prompt_tokens` is the whole prompt with the cached part inside it, and `usageFromWire` stored it whole beside `cached_tokens`, so `cacheRead / (cacheRead + input)` put the cached half in the denominator as well. A true 90% hit displayed as 47.4%, and no OpenAI session could report above 50% however well the cache worked. Anthropic reports the two apart, so the same stored field meant two different things and `nh usage` added them into one total anyway. (fixed: src/providers/openai.ts normalizes `input` to the uncached half, src/providers/usage.test.ts pins both wires against the same spend.)
- [x] Cache writes were left out of the cache-hit denominator, so a turn that read 20k from cache and wrote 5k new reported 99.9% when four fifths of its prompt came from cache. A cache write is prompt the provider read in full and charged a premium for. plan.md §15 specified the formula that way, so the code was right about the plan and the plan was wrong. (fixed: `cacheHitRate` in src/core/types.ts, plan.md, docs/harness/cli.md.)
- [x] The tok/s rate had tool execution time in its denominator. A `usage` event fires when the model's stream ends, tools run after that, and the renderer took its seconds from the gap between two usage events. One 30-second bash call reported the model generating at a fraction of its real speed, under a label that says throughput. (fixed: src/core/session.ts times the stream itself, first chunk to last, and sends `streamMs` with the event; src/renderer/chat.ts accumulates tokens and generation-seconds across the turn instead of dividing by wall clock.)
- [x] A subagent's usage event reset the parent's rate clock. `addSubagentUsage` emits on the same bus, so the renderer charged the spawn's output against the interval since the last event and then restarted the clock, leaving the parent's next round with a truncated denominator and an inflated rate. Both numbers were wrong, and docs/harness/agents.md claimed the mixing was deliberate. (fixed: subagent usage carries no `streamMs`, so it adds to the counter and leaves the rate alone; docs/harness/agents.md says so.)
- [x] Three copies of the cache-hit formula: src/cli/usage.ts, and twice in src/renderer/chat.ts. They had already drifted, so the window and `nh usage` could disagree about the same turn. (fixed: one definition in src/core/types.ts for the CLI and main process, and one copy in src/renderer/metrics.ts for the window. The renderer cannot use the core one: eslint.config.js forbids it a runtime import from core, because it ships as its own bundle. The copy is no longer left to inspection - src/providers/usage.test.ts runs both against the same wire-derived spend and requires the same answer, and fails if either loses a term.)
- [x] `nh usage` printed `reasoning` as a peer of input and output, which reads as a sixth number to add on. Both wires count reasoning inside the completion tokens. (fixed: src/cli/usage.ts indents it under output.)
- [x] The usage log had no schema version, and version 2 changed what `input` counts. `nh usage` added rows written before the OpenAI fix to rows written after it, so one total mixed two meanings: the input was inflated by every cached token in the old rows and the hit rate read about half of what it should. Old rows cannot be converted, because a row does not record which wire wrote it and an Anthropic row was always in the new units. (fixed: src/core/usage-log.ts stamps `v` on write and a line from any other version is skipped, counted in the same skipped total as an unreadable one; src/core/usage-log.test.ts pins it. 2026-09-13: the separate "outdated" count was removed, since the pre-1.0 project keeps no compatibility path. A session total stored in `workspaces.json` before the change keeps its overlap for the life of that session, which src/core/session.ts says out loud.)
- [x] A server that reported more cached tokens than prompt tokens drove `input` negative and the hit rate above 100%. Contradictory, and there is no way to tell which of the two numbers is wrong. (fixed: src/providers/openai.ts caps the cached count at the prompt, so the rate stays inside 0..1; src/providers/usage.test.ts covers that and the order the two cached-token spellings are read in. Superseded 2026-09-13: the cap was a number true under nothing. `parseUsage` rejects a usage report missing its totals or contradicting itself, the done chunk carries the reason, and src/core/session.ts records one fault saying the cost is unknown while keeping the answer. The rest of the validation moved into `parseUsage`, which resolved `?? 0` fallbacks that would have hidden the same class of bad report.)
- [x] The cache hit rate could not be checked against the numbers printed beside it. `cacheWrite` is in the denominator but appeared in no pill, no tooltip and no per-model row, so an Anthropic turn showing 80% had nothing on screen that divides to 80%. (fixed: src/renderer/chat.ts and src/cli/usage.ts print a cache write wherever there is one, and leave it out where there is none, since only Anthropic ever reports one.)
- [x] docs/harness/tools.md said `read` and `write` were "not sandboxed" and that scope was "not a security boundary". Both false since the `AccessGate` landed: every tool is handed the gate and src/tools/read.ts, src/tools/write.ts and src/tools/bash.ts all call it. The doc is read by agents deciding what they may touch, so it was telling them the file tools were unbounded. (fixed: the section now describes the gate and points at sessions.md for the rule.)
- [ ] `nh usage` reports no tokens-per-second at all. `streamMs` exists per round and is thrown away at the end of the turn, so the log cannot answer which model or which provider was actually faster (src/core/usage-log.ts, src/cli/usage.ts).
- [x] The deadline test in src/mcp/client.test.ts failed about half the time once the worker pool was busy. Not the deadline: the cancel notification is sent without being awaited, because the request rejects on its own timer rather than on the server's acknowledgement, so the reject and the notification race and the test read the server's log before the notification landed in it. (fixed: the test polls the log for that one message with a 2s ceiling instead of reading it once. Four consecutive runs green.)
- [ ] Nothing sends `cache_control` on the Anthropic wire, so `cacheRead` stays zero there and the hit rate reads 0.0% no matter what the prompt looks like. The metric is now correct and still has nothing to measure on that provider (src/providers/anthropic.ts). Same entry as 2026-09-04; it is what makes the cache-hit work above half-finished.

## 2026-09-13

- [x] Git Bash cuts a `-c` string at 8192 bytes and runs what is left, so any command over that size executed half written with nothing saying so. A 12 KB heredoc that patched `scenery.js` arrived cut at line 166: bash warned about an unterminated heredoc, python died on an unterminated string, and the agent read the pair as a failed edit rather than an edit that had already half happened. A `rm` or a `git` chain in the same shape would run its front half for real. (fixed: src/tools/bash.ts writes the command to a temporary script and runs `bash -l <script>`, which has no such limit, and folds CRLF to LF so a heredoc terminator still matches; src/tools/bash.test.ts runs a 12 KB command end to end.)
- [x] Any shell word starting with `/` was read as an absolute path, so command text was refused as filesystem access. `sed -n '/faces/,/steering/p'` produced a permission prompt for `C:\faces\,\steering\p`, and a JS `//` comment inside a heredoc produced one for a place spelled out of the sentence in the comment. The user answers a modal naming somewhere that has never existed, the command does not run, and the agent spends its next turns looking for the edit it thinks it made. (fixed: `looksLikePosixPath` in src/core/scope.ts requires a word to read as a path throughout, with a first segment that is a Git Bash drive, a system directory or a directory that is there; src/core/scope.test.ts pins the three shapes. Superseded: the screen itself was removed; see the entry on the run that lost six commands.)
- [x] A command stopped by the scope check reported only the path, so an agent could not tell a refusal from a failure and did not know its edit had never run. One session lost five round trips checking a file for text a blocked patch had never written. (fixed: src/tools/bash.ts says the command did not run and lists the words that were read as paths. Superseded: nothing stops a command for a path anymore.)
- [x] Every refusal said "you denied access to it". A window that went away produces the same refusal with nobody having answered anything, and the wording reads as a refusal of that one path, so an agent refused `chrome.exe` asked next about the puppeteer cache and then the playwright cache, one modal per attempt. (fixed: src/main/permission.ts says access was refused and that another route to the same place stops the turn again; a path already refused this session is answered from the earlier answer and says so.)
- [x] A background subagent still running when the app closed died in silence. One session ended with the agent saying a reviewer's report would arrive, and the reopened conversation held no report and no reason; the job registry is in memory by design and nothing wrote the loss down. (fixed: `JobRegistry.abandon` in src/core/jobs.ts ends what is running as `stopped`, `abandonJobs` in src/main/index.ts delivers one message per background job into the conversation that started it and stores the transcript, and the `spawn` description in src/tools/spawn.ts says a job only survives while the app is open.)
- [x] A heredoc body was read as if it were command text, and a `/`-leading word whose parent is the root of a drive was read as a path. A run that wrote a bundler through a `python - <<'PYEOF'` heredoc was refused for `C:\script` and `C:\body`: the tail of `'</script>'` and `'</body>'` is `/script` and `/body`, and `C:\` exists on every Windows machine, so the existence check said yes. The command did not run, and the person at the keyboard answered a prompt naming two directories nobody has. (fixed: `withoutHeredocBodies` in src/core/scope.ts drops every heredoc body before the words are split, which also keeps an apostrophe in a comment from opening a quote that runs to the end of the command, and `looksLikePosixPath` no longer counts the root of a drive as a parent; src/core/scope.test.ts runs the command that failed and checks that the file the heredoc is written into is still asked about. Superseded: there is no word splitter left to patch.)
- [x] The prompt said nothing about having no eyes, so a run whose first line ruled out browser tools opened with probes for Chrome, Edge, puppeteer and playwright, and carried on probing through two interruptions asking why. Three of the four probes were paths outside the workspace, so each one stopped the turn with a modal. (fixed: src/core/prompt.ts says there is no screen, no browser and no image here and that work is checked by running it; that a limit the user states is part of the task, including going to look for a ruled-out tool; and that an interruption is answered in words before anything else runs.)
- [x] One run lost six commands to the scope screen, all of them to paths the commands were not going to. A three.js scene built through Python heredocs: a bundler patch refused for `C:\script` and `C:\body` (the tails of `'</script>'` and `'</body>'` in the body), a terrain patch refused for `C:\dev\null` (`2>/dev/null` inside a body, which the embedded-path shape matched before the device-node filter ever saw the token), a README edit refused for `e://` (a URL inside a string), and a browser probe refused for Chrome and Edge under `Program Files`, one prompt each. Every refusal stopped a command that had nothing to do with the path named, and the agent then spent turns checking an edit that had never run. (fixed: the command line is not read for paths at all, which is the deepseek-harness approach: resolve a path where it is used, never guess one out of a string. src/core/scope.ts is resolution and containment only; src/tools/bash.ts asks the gate to approve the command whole before it runs, the app gate shows the person the command and offers allow once, allow all shell commands for the session, or deny, and a gate with nobody to ask refuses it.)
- [ ] The zoo run spent 179 tool calls on one scene. Almost all of them were `bash`: there was no `edit` tool, so every patch was a Python heredoc through the shell, one round trip each; the six refusals above were six more calls that could not do their work; and several reads in one message still ran one at a time. (partly fixed: `edit` landed with the literal unique-match semantics of deepseek-harness, including CRLF-safe matching, fatal UTF-8 instead of a lossy decode, and a refusal when the two strings are equal; one divergence remains, the write is a direct `writeFile` where the reference stages and replaces, and there is no abort signal to thread yet. `read` declares itself parallel and the session loop runs a message's read-only calls together, with writes still exclusive and every result committed in the model's order; a tool that throws becomes a failed result instead of killing the turn and every sibling call's answer with it. The prompt now asks for independent calls in one message and says not to read a file back after a change. What that cuts a scene-sized run to is unmeasured.)
- [ ] Shell approval covers the `bash` tool, not every command the harness runs. An MCP stdio server is spawned from `.nanoharness/mcp.json` when a session is built (src/mcp/transport.ts, src/main/index.ts), and any role with `write` can author that file inside the workspace without a prompt, so a server entry is a command the person never saw. MCP servers are meant to be user-configured; if the agent may configure them, that file needs the approval the shell just got.
